# Bash with blesh for syntax highlighting and zsh-like prompt
# Install blesh: git clone https://github.com/akinomyoga/ble.sh.git && make -C ble.sh install PREFIX=~/.local
# Add to ~/.bashrc: source ~/.local/share/blesh/ble.sh
shell /bin/bash
